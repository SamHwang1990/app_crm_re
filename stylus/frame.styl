html
  width 100%
  height 100%
  font-size 125% // 20px % 16px, the minimum font-size is 12px in browser, so i set the 20px as the basis

body
  width 100%
  font-family Helvetica, Arial, sans-serif
  color ($gray)
  font-size 16px
  font-size .8rem
  background-color #ffffff

/*!
* login Module
*/
body.body-login
  height 100%
  @media (min-width: $screen-sm-min)
    // 下面的min-height: 636px 不是完全计算出来的，而是调了好几次数值，觉得这个数值的效果比较好
    // 分开目的：浏览器高度太小，影响flex 的效果，导致login-main-header 的内容有部分被隐藏了
    // 所以，此时只设置padding-top
    @media screen and (min-height: 636px)
      display flex
      align-items center

    @media screen and (max-height: 635px)
      padding-top 15px

/*!
* ac-main Module
*/
.ac-main
  margin 0
  width 100%
  height auto

